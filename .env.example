# API & WebSocket
NUXT_PUBLIC_API_BASE_URL=http://localhost:3001
NUXT_PUBLIC_WS_URL=http://localhost:3001

# Link preview (jsonlink.io — free tier, no key needed; add key for higher rate limits)
NUXT_PUBLIC_JSON_IO_LINK_PREVIEW_API_KEY=

# Feature flags (set to "true" to enable)
# NUXT_PUBLIC_FF_NEW_DASHBOARD=false
